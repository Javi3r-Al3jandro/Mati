import Button from "./Button/button";
import Input from "./input/input"
import Background from "./background/Background";


//Button va entre llaves porque lo que exportamos es un objeto
export {Button , Input, Background}

//Este archivo se utiliza para que maneje todos los componentes y no se recargue de import nuestra APP
